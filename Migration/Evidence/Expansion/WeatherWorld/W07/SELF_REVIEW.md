# W07 diagnostic, not final delivery

Actual 17 FHD captures show a more continuous ridge and seated roof, orange cracks removed, and visible timber connections. W06 source/scene/build and all original collision/transforms remain preserved.

Do not claim the thunder request passed: W06/W07 peak image luminance was essentially unchanged despite larger runtime ColorAdjustments values. Measured default peaks 74.458/74.179 and stronger 86.796/86.537 in 8-bit grayscale. These averages are image values, not physical luminance. Extra configured exposure was not an effective rendered change. Exact cause within the render path was not established; no broad pipeline repair is authorized here.

Continue in a separate W08 scene/build/evidence with a bounded screen-space multiplicative pass. This must visibly increase final output, preserve directionless two-pulse timing and normal darkness. W07 is preserved as a useful structural candidate with unsuccessful flash-intensity change. `build.log` contains the first editor helper compile error; `build-02.log` and `build.json` are the successful 0-error/0-warning build. `source/` is the first attempt; `source-final/` is the built source.
